// Reset board
$(".cell").removeClass("marked_x").removeClass("marked_o");

// Fetch player name
var current_player_name = $("#player<%= @current_player %>_name").text();

// Print status text
$("#status_field").text("Okay, lets play! " + current_player_name + " is first out.")