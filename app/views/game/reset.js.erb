// Remove all the X's/O's from the game board.
$(".cell").removeClass("marked_x").removeClass("marked_o").removeClass("disabled");

// Fetch player name and print status.
var current_player_name = $("#player<%= @current_player %>_name").text();
$("#status_field").text("Okay, lets play! " + current_player_name + " is first out.");